#include <stdio.h>
int main()
{ 
  int leaders[n];
  int i;
  int count = 0;
  int max_so_far = arr [n - 1];
  leaders [count++] = max_so_far;
  for(i = n-2;i >= 0;i--){
    if(arr[i] > max so far){
      max_so_far = arr[i];
      leaders[count++] = arr[i];
    }
  }
  for (i = count -1;i>= 0;i--){
    printf("%d",leaders[i]);
  }
  return 0;
}
c